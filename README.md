# Виджет «Операции по счетам»

Проект представляет собой backend-реализацию алгоритма, который подготавливает данные для виджета «Операции по счетам» в личном кабинете клиента банка. Виджет отображает **5 последних выполненных операций (EXECUTED)** с корректным форматированием даты, счета, карт и суммы.

---

## 🚀 Функциональность
- Чтение данных из файла `operations.json`.
- Фильтрация только выполненных операций (`state = EXECUTED`).
- Сортировка операций по дате — сначала самые новые.
- Маскирование номеров карт и счетов:
  - **Карты:** `XXXX XX** **** XXXX` (видно первые 6 и последние 4 цифры).
  - **Счета:** `**XXXX` (видно только последние 4 цифры).
- Форматирование даты в формат `ДД.ММ.ГГГГ`.
- Вывод **5 последних успешных операций**.
- Красивый вывод с разделением пустой строкой.

---

## 📄 Пример вывода
```
14.10.2018 Перевод организации
Visa Platinum 7000 79** **** 6361 -> Счет **9638
82771.72 руб.
```

---

## 📂 Структура проекта
```
project/
├── operations.json        # данные по операциям (исходный файл)
├── main.py    # точка входа
├── tests/     # тесты к программе  
├── requirements.txt       # зависимости
├── README.md              # документация проекта
└── .gitignore
```

---

## 🧠 Логика работы
1. Файл `operations.json` считывается через библиотеку `json`.
2. Все операции фильтруются по `state == "EXECUTED"`.
3. Список сортируется по полю `date` в порядке убывания.
4. Оставляются только первые 5 операций.
5. Для каждой операции выполняется:
   - Приведение даты к формату `ДД.ММ.ГГГГ`.
   - Маскирование номера счета/карты.
   - Формирование человекочитаемого вывода.

---

## ▶️ Запуск проекта
### 1. Создать и активировать виртуальное окружение
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Запуск программы
```bash
python main.py
```

---

## 🧪 Тестирование
В проекте используются **pytest**.

### Запуск тестов:
```bash
pytest
```
